{
  "name": "devloop",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "Mayflower GmbH",
  "repository": { "type": "git", "url": "https://github.com/mayflower/devloop.git" },
  "homepage": "https://github.com/mayflower/devloop#readme",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check:dist": "npm run build && git diff --exit-code -- dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
